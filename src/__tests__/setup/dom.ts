/**
 * DOM environment setup for React component testing using happy-dom
 */
import { GlobalRegistrator } from "@happy-dom/global-registrator";

// Register happy-dom globals (window, document, etc.)
GlobalRegistrator.register();
